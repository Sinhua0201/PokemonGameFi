# 🚀 Pokemon NFT 合约部署说明

## 当前状态

- ✅ 合约已构建成功
- ✅ 你的新钱包地址: `0x7c24267e7c7babae39987dcad1f52334f8cf94455f08e47f1072ebd2f233f06d`
- ⚠️ Sui 配置文件损坏，需要修复

## 问题说明

你的 Sui CLI 配置文件 (`C:\Users\User\.sui\sui_config\client.yaml`) 损坏了，导致无法执行部署命令。

## 解决方案

### 方案 1: 修复配置文件（推荐）

运行修复脚本：
```powershell
cd contracts\pokemon_nft
.\修复配置并部署.ps1
```

这个脚本会：
1. 备份并删除损坏的配置文件
2. 重新初始化 Sui 客户端
3. 引导你完成配置

**重要提示：**
- 会生成新的助记词，请务必记下来！
- 如果你想使用现有钱包，需要导入助记词

### 方案 2: 手动修复

1. **删除损坏的配置文件**
   ```powershell
   Remove-Item "$env:USERPROFILE\.sui\sui_config\client.yaml" -Force
   ```

2. **重新初始化**
   ```powershell
   sui client
   ```
   
   按提示操作：
   - 连接到 Sui Full node? `y`
   - 服务器 URL? `https://fullnode.testnet.sui.io:443`
   - 环境别名? `testnet`
   - 密钥方案? `0` (ed25519)

3. **导入你的钱包（如果有助记词）**
   ```powershell
   sui keytool import "<你的助记词>" ed25519
   ```

4. **切换到你的地址**
   ```powershell
   sui client switch --address 0x7c24267e7c7babae39987dcad1f52334f8cf94455f08e47f1072ebd2f233f06d
   ```

### 方案 3: 使用 OneChain（不推荐）

OneChain 目前 API 版本太旧（1.0.1），无法部署现代 Move 合约。建议先在 Sui Testnet 测试。

## 部署步骤

配置修复后：

### 1. 获取测试币

**Sui Testnet:**
1. 访问 https://discord.com/invite/sui
2. 在 #testnet-faucet 频道发送：
   ```
   !faucet 0x7c24267e7c7babae39987dcad1f52334f8cf94455f08e47f1072ebd2f233f06d
   ```

### 2. 检查余额

```powershell
sui client gas
```

确保有足够的 SUI（至少 0.5 SUI）

### 3. 部署合约

```powershell
cd contracts\pokemon_nft
.\立即部署.ps1
```

或手动部署：
```powershell
sui client publish --gas-budget 500000000
```

### 4. 保存部署信息

部署成功后，会生成 `deployment_info.txt` 文件，包含：
- Package ID
- 部署时间
- 网络信息
- 下一步操作指南

## 更新配置

部署成功后，需要更新以下文件：

### frontend/.env.local
```bash
NEXT_PUBLIC_PACKAGE_ID=<新的 Package ID>
NEXT_PUBLIC_MARKETPLACE_ID=<MarketplaceConfig ID>
```

### backend/.env
```bash
PACKAGE_ID=<新的 Package ID>
MARKETPLACE_ID=<MarketplaceConfig ID>
```

## 测试合约

部署后测试铸造 Pokemon：

```bash
sui client call \
  --package <PACKAGE_ID> \
  --module pokemon \
  --function mint_starter \
  --args 1 "Pikachu" 1 25 0x7c24267e7c7babae39987dcad1f52334f8cf94455f08e47f1072ebd2f233f06d \
  --gas-budget 10000000
```

## 查看区块浏览器

**Sui Testnet:**
```
https://suiscan.xyz/testnet/object/<PACKAGE_ID>
```

## 故障排除

### 配置文件被锁定

如果看到 "The process cannot access the file" 错误：
1. 关闭所有 PowerShell 窗口
2. 重新打开 PowerShell
3. 再次运行修复脚本

### 余额不足

```powershell
# 检查余额
sui client gas

# 如果余额为 0，请求测试币
# 访问 Sui Discord
```

### 网络连接问题

```powershell
# 检查网络
sui client active-env

# 切换到 Sui Testnet
sui client switch --env testnet
```

### OneChain 兼容性问题

如果必须使用 OneChain：
1. 联系 OneChain 团队确认 API 版本
2. 等待 OneChain 升级到至少 1.30+ 版本
3. 或者先在 Sui Testnet 测试

## 需要帮助？

如果遇到问题：
1. 查看错误信息
2. 检查配置：`sui client active-env`
3. 检查余额：`sui client gas`
4. 查看日志文件

---

**准备好了吗？开始修复配置：**

```powershell
cd contracts\pokemon_nft
.\修复配置并部署.ps1
```
