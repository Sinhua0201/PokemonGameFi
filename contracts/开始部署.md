# 🚀 快速开始 - 部署智能合约

## 📋 你现在的状态

✅ 已安装 Sui CLI 1.59.1

## 🎯 接下来的 3 个步骤

### 步骤 1: 配置 Sui CLI (5 分钟)

打开 PowerShell，运行：

```powershell
# 检查 Sui 是否在 PATH 中
sui --version
```

**如果显示版本号**：✅ 继续下一步

**如果显示 "command not found"**：
1. 找到 `sui.exe` 的位置（通常在 `C:\Users\你的用户名\.cargo\bin\`）
2. 添加到 PATH（查看 `WINDOWS_SETUP_CN.md` 的详细说明）
3. 重新打开 PowerShell

### 步骤 2: 初始化钱包 (3 分钟)

```powershell
# 初始化 Sui 客户端
sui client

# 切换到测试网
sui client switch --env testnet

# 获取测试币
sui client faucet

# 检查余额（等待 10 秒后）
sui client gas
```

### 步骤 3: 部署合约 (2 分钟)

```powershell
# 进入 contracts 目录
cd contracts

# 运行检查（可选但推荐）
.\check_contracts.bat

# 运行测试（可选但推荐）
.\test_contracts.bat

# 部署！
.\deploy.bat
```

## 📝 部署成功后

部署成功后，你会看到类似这样的输出：

```
Package ID: 0x1234567890abcdef...
```

**重要**：复制这个 Package ID！

## ⚙️ 配置前端

编辑 `frontend/.env.local`：

```env
NEXT_PUBLIC_ONECHAIN_PACKAGE_ID=0x你复制的Package_ID
NEXT_PUBLIC_ONECHAIN_NETWORK=testnet
```

## 🎮 启动游戏

```powershell
# 启动后端
cd backend
python -m uvicorn main:app --reload

# 新开一个终端，启动前端
cd frontend
npm run dev
```

访问：http://localhost:3000

## 🆘 遇到问题？

### 问题 1: "sui: command not found"
👉 查看 `WINDOWS_SETUP_CN.md` 的 "步骤 1: 添加 Sui 到 PATH"

### 问题 2: "insufficient gas"
```powershell
sui client faucet
# 等待 10 秒
sui client gas
```

### 问题 3: 编译错误
```powershell
cd pokemon_nft
sui move build
# 查看详细错误信息
```

### 问题 4: 部署失败
1. 确认余额充足：`sui client gas`
2. 确认网络正确：`sui client active-env`
3. 重试部署：`.\deploy.bat`

## 📚 详细文档

- `WINDOWS_SETUP_CN.md` - Windows 完整配置指南
- `CONTRACT_STATUS_CN.md` - 合约功能说明
- `DEPLOYMENT_GUIDE_CN.md` - 详细部署指南

## ✅ 检查清单

部署前确认：
- [ ] Sui CLI 已安装：`sui --version`
- [ ] 钱包已配置：`sui client active-address`
- [ ] 网络是测试网：`sui client active-env`
- [ ] 有测试币：`sui client gas`
- [ ] 合约编译成功：`cd pokemon_nft && sui move build`

全部打勾？运行 `.\deploy.bat` 开始部署！

---

**需要帮助？** 查看详细文档或在 GitHub 提 Issue
