# 🎨 3D 模型资源下载指南

## 📦 推荐的免费 GLB/GLTF 模型网站

### 1. **Sketchfab** ⭐⭐⭐⭐⭐
- 网址: https://sketchfab.com
- 特点: 最大的 3D 模型库，有免费下载选项
- 搜索关键词: "cartoon", "low poly", "stylized", "pokemon style"
- 筛选: 勾选 "Downloadable" 和 "Free"
- 格式: 支持 GLB/GLTF 导出

### 2. **Poly Pizza** ⭐⭐⭐⭐⭐
- 网址: https://poly.pizza
- 特点: 完全免费，低多边形风格，适合游戏
- 风格: 卡通、简约、低多边形
- 格式: GLB 格式

### 3. **Quaternius** ⭐⭐⭐⭐⭐
- 网址: https://quaternius.com
- 特点: 完全免费，专注游戏资源
- 风格: 低多边形、卡通风格
- 包含: 角色、动物、建筑、道具
- 格式: GLB/FBX

### 4. **Kenney Assets** ⭐⭐⭐⭐
- 网址: https://kenney.nl/assets
- 特点: 游戏开发资源，CC0 许可
- 风格: 简约、卡通
- 格式: GLB/OBJ

### 5. **CGTrader Free** ⭐⭐⭐⭐
- 网址: https://www.cgtrader.com/free-3d-models
- 特点: 有免费模型区
- 筛选: 选择 "Free" 和 "GLB/GLTF"
- 质量: 高质量模型

### 6. **Free3D** ⭐⭐⭐
- 网址: https://free3d.com
- 特点: 免费 3D 模型
- 格式: 多种格式，需要转换

## 🎮 推荐搜索关键词

### 角色类
- "low poly character"
- "cartoon character"
- "stylized character"
- "chibi character"
- "anime character"

### 动物类
- "low poly animals"
- "cartoon animals"
- "stylized creatures"
- "pokemon style"

### 环境类
- "low poly tree"
- "cartoon building"
- "stylized rock"
- "game props"

## 📁 项目文件结构

创建以下文件夹结构：

```
frontend/
└── public/
    └── assets/
        └── models/
            ├── characters/
            │   ├── player.glb
            │   └── npc.glb
            ├── environment/
            │   ├── tree.glb
            │   ├── rock.glb
            │   └── flower.glb
            └── creatures/
                ├── pokemon1.glb
                └── pokemon2.glb
```

## 🔧 如何在代码中使用 GLB 模型

### 1. 安装 GLB 加载器（已安装）
```bash
npm install @react-three/drei
```

### 2. 使用 useGLTF 加载模型

```typescript
import { useGLTF } from '@react-three/drei'

function TreeModel({ position }: { position: [number, number, number] }) {
  const { scene } = useGLTF('/assets/models/environment/tree.glb')
  
  return (
    <primitive 
      object={scene.clone()} 
      position={position}
      scale={1}
      castShadow
      receiveShadow
    />
  )
}

// 预加载模型
useGLTF.preload('/assets/models/environment/tree.glb')
```

### 3. 完整示例

```typescript
import { useGLTF } from '@react-three/drei'

// 树木组件
function Tree3D({ position }: { position: [number, number, number] }) {
  const { scene } = useGLTF('/assets/models/environment/tree.glb')
  
  return (
    <primitive 
      object={scene.clone()} 
      position={position}
      scale={[1.5, 1.5, 1.5]}
      castShadow
      receiveShadow
    />
  )
}

// 石头组件
function Rock3D({ position }: { position: [number, number, number] }) {
  const { scene } = useGLTF('/assets/models/environment/rock.glb')
  
  return (
    <primitive 
      object={scene.clone()} 
      position={position}
      scale={0.8}
      castShadow
    />
  )
}

// 玩家角色
function Player3D({ position }: { position: [number, number, number] }) {
  const { scene } = useGLTF('/assets/models/characters/player.glb')
  
  return (
    <primitive 
      object={scene.clone()} 
      position={position}
      scale={1}
      castShadow
    />
  )
}

// 预加载所有模型
useGLTF.preload('/assets/models/environment/tree.glb')
useGLTF.preload('/assets/models/environment/rock.glb')
useGLTF.preload('/assets/models/characters/player.glb')
```

## 🎯 更新 Explore3DScene.tsx

将现有的简单几何体替换为 GLB 模型：

```typescript
// 在文件顶部导入
import { useGLTF } from '@react-three/drei'

// 替换 Tree 组件
function Tree({ position }: { position: [number, number, number] }) {
  const { scene } = useGLTF('/assets/models/environment/tree.glb')
  return <primitive object={scene.clone()} position={position} scale={1.5} castShadow />
}

// 替换 Rock 组件
function Rock({ position }: { position: [number, number, number] }) {
  const { scene } = useGLTF('/assets/models/environment/rock.glb')
  return <primitive object={scene.clone()} position={position} scale={0.8} castShadow />
}

// 替换 Player 组件
function Player({ position }: { position: [number, number, number] }) {
  const { scene } = useGLTF('/assets/models/characters/player.glb')
  return <primitive object={scene.clone()} position={position} scale={1} castShadow />
}

// 在组件外预加载
useGLTF.preload('/assets/models/environment/tree.glb')
useGLTF.preload('/assets/models/environment/rock.glb')
useGLTF.preload('/assets/models/characters/player.glb')
```

## 🔄 格式转换工具

如果下载的模型不是 GLB 格式：

### 在线转换
- **glTF Viewer**: https://gltf-viewer.donmccurdy.com
- **Blender** (免费软件): 导入 → 导出为 GLB

### 命令行工具
```bash
npm install -g gltf-pipeline
gltf-pipeline -i model.gltf -o model.glb
```

## ⚡ 性能优化建议

1. **压缩模型**: 使用 gltf-pipeline 压缩
```bash
gltf-pipeline -i input.glb -o output.glb -d
```

2. **减少多边形**: 在 Blender 中使用 Decimate 修改器

3. **优化纹理**: 
   - 使用 512x512 或 1024x1024 纹理
   - 转换为 WebP 格式

4. **预加载**: 使用 `useGLTF.preload()` 提前加载

5. **实例化**: 对重复模型使用 `scene.clone()`

## 📝 推荐模型包

### Quaternius 推荐包
- **Ultimate Low Poly Nature Pack**: 树木、石头、花草
- **Ultimate Low Poly Characters**: 卡通角色
- **Ultimate Low Poly Animals**: 动物模型

### Kenney 推荐包
- **Nature Kit**: 自然环境
- **Character Pack**: 角色模型
- **Platformer Kit**: 平台游戏资源

## 🎨 风格建议

为了保持一致性，选择模型时注意：
- ✅ 低多边形风格（Low Poly）
- ✅ 卡通/风格化（Cartoon/Stylized）
- ✅ 简单纹理
- ✅ 明亮色彩
- ❌ 避免写实风格
- ❌ 避免高多边形模型

## 🚀 快速开始步骤

1. 访问 https://poly.pizza
2. 搜索 "tree", "rock", "character"
3. 下载 GLB 文件
4. 放到 `frontend/public/assets/models/` 文件夹
5. 在代码中使用 `useGLTF` 加载
6. 测试效果

立即开始打造你的 3D 世界！🎉
