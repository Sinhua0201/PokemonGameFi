# Nixpacks configuration for Railway deployment
# Make sure to set Root Directory to "backend" in Railway Settings

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
